import { IDefinitions } from './baseInterfaces';
export interface IDefinitionsClasses {
    [key: string]: {
        isGeneric: boolean;
        value: string;
    };
}
export declare function definitionsCodeGen(definitions: IDefinitions): string;
